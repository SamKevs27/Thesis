with open("static/style.css", "r") as f:
    css = f.read()

# I will replace CSS rules to make it look minimal, soft, modern, without touching HTML structure if possible
# Let's actually fully rewrite the CSS while keeping the same IDs and class names.
